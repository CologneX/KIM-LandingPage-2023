"use client";
import { NavbarMenuToggle } from "@nextui-org/react";

export default () => {
  return (
    <NavbarMenuToggle
      onChange={(isOpen) => {
        // close NavbarMenu chen isOpen is false
            
        
      }}
    />
  );
};
